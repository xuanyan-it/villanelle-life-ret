services:
  web:
    ports:
      - "80:80"
      - "443:443"
    environment:
      NGINX_SSL_ENABLED: "true"
    # Certbot usually writes everything under /etc/letsencrypt (certs + options + dhparam).
    volumes:
      - ${LETSENCRYPT_PATH:-/etc/letsencrypt}:/etc/letsencrypt:ro

